import 'package:flutter/material.dart';

class PoliWebView extends StatelessWidget {
  const PoliWebView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
